{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "node scripts/switch-db-provider.js && prisma generate && prisma db push --accept-data-loss && next build",
  "installCommand": "bun install",
  "functions": {
    "src/app/api/admin/upload/route.ts": {
      "maxDuration": 30
    }
  }
}
